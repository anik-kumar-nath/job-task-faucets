import React from 'react';
import Header from '../Header/Header';
import Login from '../Header/Login';

const Home = () => {
    return (
        <div>
            <Header></Header>
            <Login></Login>
        </div>
    );
};

export default Home;